defaultsdir=$(pkgdatadir)/defaults
defaults_DATA=

install-data-hook:
	-cp -rf preferences $(defaultsdir)
	-ln -s -f $(defaultsdir) ${pkglibdir}/defaults

uninstall-hook:
	-rm -rf $(defaultsdir) ${pkglibdir}/defaults
