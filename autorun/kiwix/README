Procedure to compile your own version of kiwix autorun.
This general procedure is to compile the autorun kiwix (launcher). 
It's been done in c++ with Qt libraries.

==Windows OS plataform==

Requeriments:

*moc tool. (https://kiwix.svn.sourceforge.net/svnroot/kiwix/moulinkiwix/autorun/kiwix/bin/moc.exe)
*qt 2010.01 (http://qt.nokia.com/downloads/sdk-windows-cpp)
*Msys (http://sourceforge.net/projects/mingw/files/)

Procedure:

1) install Msys
2) install qt 2010.01
3) copy moc.exe to qt 2010.01 bin folder, the default folder is "C:\Qt\2010.01\qt\bin"

Compile kiwix launcher:

1) run Msys, search it in Start-> Programs-> MinGW-> MSYS-> MSYS
2) go to autorun source folder. example: "$ cd /c/moulinkiwix/autorun/kiwix/"
3) run ls command "$ ls"
ensure that the autorun.pro file is in the list.
the result should be something similar.
----
$ ls
Makefile        Makefile.Release  README       bin    debug    src  ui_app.h
Makefile.Debug  Makefile_windows  autorun.pro  build  release  ui   ui_source.h
----
4) run "$ qmake autorun.pro"
5) run "$ make"

Ready!. Into the new folder called "debug", 
"debug" folder is located in the same place "autorun.pro". 
You must find your own "autorun.exe"